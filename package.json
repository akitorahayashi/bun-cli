{
  "name": "bun-cli",
  "version": "0.1.0",
  "description": "Bun and TypeScript CLI template",
  "packageManager": "bun@1.3.14",
  "module": "src/main.ts",
  "type": "module",
  "private": true,
  "bin": {
    "bun-cli": "./src/main.ts"
  },
  "scripts": {
    "e": "bun src/main.ts",
    "up": "bun scripts/install-bun-cli.ts",
    "prebuild": "mkdir -p dist",
    "build": "bun build src/main.ts --target bun --outfile dist/bun-cli",
    "test": "bun test",
    "test:unit": "bun test src",
    "test:integration": "bun test tests",
    "typecheck": "tsc --noEmit",
    "check": "biome check . && bun run typecheck",
    "fix": "biome check --write .",
    "prepare": "husky"
  },
  "dependencies": {
    "clipanion": "^4.0.0-rc.4"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.15",
    "@types/bun": "1.3.14",
    "husky": "9.1.7",
    "typescript": "6.0.3"
  }
}
