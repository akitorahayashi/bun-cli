{
  "name": "bun-cli",
  "version": "0.1.0",
  "description": "Bun and TypeScript CLI template",
  "packageManager": "bun@1.3.14",
  "module": "src/main.ts",
  "type": "module",
  "private": true,
  "bin": {
    "bun-cli": "./src/main.ts"
  },
  "scripts": {
    "bun-cli": "bun src/main.ts",
    "prebuild": "mkdir -p dist",
    "build": "bun build src/main.ts --compile --outfile dist/bun-cli && rm -f .*.bun-build",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "check": "biome check . && bun run typecheck",
    "fix": "biome check --write .",
    "prepare": "husky"
  },
  "dependencies": {
    "@oclif/core": "^4.11.11",
    "clipanion": "^4.0.0-rc.4"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.15",
    "@types/bun": "1.3.14",
    "husky": "9.1.7",
    "typescript": "6.0.3"
  }
}
